DATABASE_URL="postgresql://user:password@localhost:5432/financedb_test"
JWT_SECRET="test-super-secret-jwt-key"
JWT_EXPIRES_IN="1h"
PORT=3001
NODE_ENV="test"
# Low value intentional for faster test runs (use 12+ in production)
BCRYPT_ROUNDS=4
